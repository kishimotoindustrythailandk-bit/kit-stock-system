# KIT Delivery Due Control v2.9.1 — schema มีแหล่งอ้างอิงเดียว

รุ่นนี้ไม่ได้เพิ่มฟีเจอร์ใหม่ แต่ทำให้ "โครงสร้างฐานข้อมูลที่รีโปบอก" ตรงกับ
"โครงสร้างที่ฐานจริงมี" และเลิกสร้างตารางตอน runtime

## ติดตั้งหรืออัปเกรด

**ฐานที่รันโค้ด v2.9.0 อยู่แล้ว** (รวมถึง Worker `kit-stock-system` ตัวจริง):

```
npm install
npm run db:baseline:runtime-ddl   # ครั้งเดียว บอก wrangler ว่าคอลัมน์ location มีอยู่แล้ว
npm run db:migrate                # รัน 0014–0017 (idempotent ทั้งชุด)
npm run build                     # ต้องผ่าน
npm run lint                      # ต้องเป็น 0 error
npm run deploy
```

**ฐานที่ยังไม่เคยผ่าน v2.9** (เคยรัน `database-upgrade-*.sql` ด้วยมือ):

```
npm install
npm run db:baseline      # ครั้งเดียว บอก wrangler ว่า 0000–0009 กับ 0012 มีอยู่แล้ว
npm run db:migrate       # รัน 0010, 0011, 0013–0018
npm run build
npm run lint
npm run deploy
```

> `npm run check` ยัง **ไม่ผ่าน** บน `main` ตั้งแต่ก่อนรุ่นนี้ (typecheck 13 error,
> test fail 8/20) รายละเอียดอยู่ในหัวข้อ "ยังไม่ได้แก้ในรุ่นนี้" ด้านล่าง
> จึงยังใช้ `npm run check` เป็นประตูก่อน deploy ไม่ได้ ให้ใช้ `build` + `lint` ไปก่อน

**ฐานข้อมูลใหม่ที่ยังว่าง** — ข้าม baseline ทั้งสองตัว แล้วรัน `npm run db:migrate` ได้เลย

ไม่แน่ใจว่าฐานอยู่สถานะไหน ตรวจได้จาก:

```bash
npx wrangler d1 execute DB --remote --command "SELECT name FROM d1_migrations ORDER BY id"
npx wrangler d1 execute DB --remote --command "PRAGMA table_info(stock_parts)"
```

ถ้า `stock_parts` มีคอลัมน์ `location` แล้ว ให้ใช้ชุดคำสั่งแรก

> **สำคัญ:** ต้องรัน migration ให้ครบก่อน deploy เสมอ v2.9.1 ถอด CREATE TABLE
> และ ALTER TABLE ออกจาก route handler แล้ว จึงไม่มีอะไรสร้างตารางให้ตอน runtime อีก

## สิ่งที่แก้ใน v2.9.1

**schema มีแหล่งอ้างอิงเดียว**

- `app/api/stock/route.ts` เดิมยิง `CREATE TABLE` / `CREATE INDEX` 6 คำสั่งผ่าน
  `DB.batch()` บวก `CREATE TABLE` ของ `stock_receipt_adjustments`,
  `stock_job_closures` และ `PRAGMA table_info` + `ALTER TABLE` ของ
  `stock_parts.location` **ทุกครั้งที่มี request เข้ามา** ทั้งใน GET และ POST
  `app/api/replacements/route.ts` และ `app/api/part-images/route.ts` ก็ทำแบบเดียวกัน
  ตอนนี้ถอดออกหมดแล้ว ทุก request ไม่ต้องจ่ายค่า DDL อีก
- `stock_receipt_adjustments` กับ `stock_job_closures` เดิมไม่มีอยู่ทั้งใน
  `migrations/` และ `db/schema.ts` เลย มีแต่ใน route handler จึงไม่มีทางรู้จากรีโป
  ได้ว่าฐานจริงมีตารางอะไร ตอนนี้อยู่ใน `migrations/0017` และ `db/schema.ts`
- คอลัมน์ `stock_parts.location` ย้ายไปอยู่ใน `migrations/0018` และประกาศใน
  `db/schema.ts` แล้ว
- เพิ่ม index `idx_stock_job_closures_job_material` ที่ฐาน production ไม่มี
  เพราะโค้ดที่สร้างตารางไม่ได้สร้าง index ให้ ทั้งที่ `close_job` อ่านด้วย
  `job_no` + `material_code` ทุกครั้ง

**ชื่อ Worker ใน wrangler.jsonc**

- เดิมเป็น `kit-stock` ซึ่งไม่มี Worker ชื่อนี้อยู่บน account เลย Worker ที่ให้บริการ
  จริงชื่อ `kit-stock-system` ใครเช็คเอาต์รีโปแล้วสั่ง `npm run deploy` จะได้ Worker
  ตัวใหม่ที่ผูก D1 production ตัวเดียวกัน กลายเป็นสองระบบเขียนฐานเดียว

**โครงสร้างรีโป**

- ไฟล์เดิมซ้อนอยู่ใต้ `kit-stock-system-v1/kit-stock-system-v1/` สองชั้น ซึ่งเป็น
  สิ่งที่ README ของตัวเองเตือนห้ามทำ ย้ายขึ้นมาที่รากรีโปแล้ว

## ยังไม่ได้แก้ในรุ่นนี้

- ตาราง `stock_manual_receipts`, `stock_count_adjustments`, `replacement_*` และ
  `part_actual_images` บนฐาน production **ไม่มี FOREIGN KEY และ CHECK** ตามที่
  migration 0014–0016 เขียนไว้ เพราะถูกสร้างจาก route handler รุ่นเก่าที่ไม่ได้ใส่
  เอาไว้ก่อน แล้ว `CREATE TABLE IF NOT EXISTS` ก็ไม่แก้ตารางที่มีอยู่แล้ว
  การเพิ่ม FK ต้อง rebuild ตารางบน production ซึ่งเป็นงานแยกอีกชุด
  migration 0017 จึงคัดลอก DDL ให้ตรงกับฐานจริงโดยเจตนา เพื่อไม่ให้ฐาน production
  กับฐานที่ติดตั้งใหม่มี schema ต่างกัน
**`npm run check` ยังไม่ผ่าน — เกิดก่อนรุ่นนี้ ไม่ได้เกิดจาก v2.9.1**

ตรวจแล้วว่าจำนวน error และรายชื่อ test ที่ fail เท่ากันทั้งบน `main` และบนรุ่นนี้

- `npm run typecheck` — 13 error
  - `app/stock-app.tsx` import `jsbarcode` แต่ `jsbarcode` ไม่มีใน `package.json`
    เลย ไฟล์นี้ (869 บรรทัด) ไม่ถูก import จากที่ไหนทั้งสิ้น จึงไม่เข้า build
    `npm run build` ผ่านได้เพราะ bundler ไม่แตะไฟล์นี้ แต่ `tsc` ตรวจทุกไฟล์
    ต้องตัดสินใจว่าจะลบไฟล์ตายทิ้ง หรือเพิ่ม `jsbarcode` เป็น dependency จริง
  - `app/delivery-control-app.tsx` 12 error `TS2367` ใน `renderScan()`
    บล็อก `if (scanMode === "arrange")` ที่บรรทัด 2574 return ออกไปก่อนแล้ว
    โค้ดหลังจากนั้น `scanMode` จึงเป็น `"dispatch"` แน่นอน แต่ยังมีเงื่อนไข
    `scanMode === "arrange" ? ก : ข` เหลืออยู่อีก 12 จุด ซึ่งเลือกข้าง `ข` เสมอ
    พฤติกรรมตอนรันถูกอยู่แล้ว เป็นเงื่อนไขตายที่ค้างจากการแยกโหมด
- `npm test` — fail 8 จาก 20 (README v2.9.0 เขียนว่า "ผ่านครบ 16/16" ซึ่งไม่จริงแล้ว)
  - `v2.8.9 separates arranging and dispatching` คาด `"arrange", "dispatch"` ติดกัน
    ใน `PERMISSION_KEYS` แต่ commit "Add replacement withdrawal permission"
    แทรก `"replacement"` เข้าไปกลางสองตัวนั้นโดยไม่แก้ test
  - อีก 7 ตัวยืนยันเนื้อหาใน `app/stock-app.tsx` ซึ่งเป็นไฟล์ตายตัวเดียวกันข้างต้น
    (พิมพ์ Tag, กล้อง, ลบ Part) test จึงตรวจโค้ดที่ไม่ได้ถูกใช้งานจริง

**อื่นๆ**

- race condition ตอนจัดงาน (`stage`) ที่ทำให้จัดเกินของจริงได้
- `DB.batch` ตอนตัดยอดไม่ได้ตรวจ `meta.changes` จึงเงียบเมื่อ guard ไม่ผ่าน
- `xlsx@0.18.5` ยังมีช่องโหว่ ต้องย้ายไป SheetJS CDN
- โค้ดตายใน `app/employees/` และตารางรุ่นเก่าใน `db/schema.ts`
- secret `SETUP_KEY` ยังอยู่บน Worker แต่ไม่มีโค้ดไหนอ่านค่านี้แล้ว

---

# v2.9.0 — ซ่อมการติดตั้ง ฐานข้อมูล และความปลอดภัยบัญชี

รุ่นนี้ไม่ได้เพิ่มฟีเจอร์ใหม่ แต่ซ่อมสิ่งที่ทำให้ติดตั้งใหม่ไม่สำเร็จมาตลอด

## สิ่งที่แก้ใน v2.9.0

**ติดตั้งใหม่แล้วใช้งานได้จริง**

- ตัวติดตั้งเดิมเรียก `database-upgrade-v2.3-part-images.sql` และ `-v2.7-stock.sql`
  ที่ไม่มีอยู่ในรีโป จึงหยุดกลางคันก่อนถึง deploy และก่อนตั้ง PIN แอดมินเสมอ
- ตาราง `part_images`, `stock_parts`, `stock_tags`, `stock_allocations`
  และ `delivery_tag_receipts` ไม่มี `CREATE TABLE` อยู่ที่ไหนเลยในรีโป
  สร้างขึ้นใหม่จาก `db/schema.ts` เป็น migration `0007`, `0008` และ `0012`
- migration เดิมแตกเป็นสองชุด (`drizzle/` กับ `migrations/`) และ `wrangler.jsonc`
  ชี้ไปที่ `drizzle/` ซึ่งเป็นสาขาที่เลิกใช้แล้ว ทำให้ `d1 migrations apply`
  ไม่เคยรัน `0004` เป็นต้นไป — ตอนนี้เหลือชุดเดียวที่ `migrations/` ครบ 0000–0013

**ความปลอดภัยบัญชี**

- ใส่การจำกัดจำนวนครั้งที่กรอก PIN ผิด: ผิด 5 ครั้งล็อก 15 นาที
  เดิมไม่มีอะไรหยุดการไล่เดา PIN 6 หลักซึ่งมีแค่ 1,000,000 ค่า
- ตอบกลับด้วยเวลาเท่ากันเมื่อไม่พบรหัสพนักงาน เพื่อไม่ให้จับเวลาแยกได้
  ว่ารหัสไหนมีอยู่ในระบบ
- เพิ่มหน้า `/change-pin` ให้ทุกบัญชีเปลี่ยน PIN ตัวเองได้ รวมถึง admin
  ซึ่งเดิมเปลี่ยนไม่ได้เลย เพราะถูก seed เป็นค่า `ENV_INITIAL_ADMIN_PIN`
  แล้วเทียบกับ environment variable แบบข้อความล้วน
- บัญชีที่ยังใช้ PIN ตั้งต้นถูกบังคับตั้ง PIN ใหม่ก่อนเข้าหน้าอื่น
  และ PIN ใหม่ถูกเก็บเป็น PBKDF2 hash
- ออกจากระบบเปลี่ยนเป็น POST เดิมเป็น GET จึงถูกเตะออกได้ด้วยการฝัง `<img>`
- ไม่ส่งข้อความ error ดิบจาก D1 กลับไปหาผู้ใช้อีก และเพิ่ม security header พื้นฐาน

**ความถูกต้องของข้อมูล**

- สแกนรับเข้า Stock ซ้ำ เดิมตอบ 201 สำเร็จทั้งที่ไม่ได้บันทึกอะไร ตอนนี้ตอบ 409
- ล้าง session ที่หมดอายุทุกครั้งที่มีคนเข้าสู่ระบบ เดิมไม่เคยล้างเลย
- เพิ่ม index บนคอลัมน์ที่ join จริง (`delivery_tag_scans.due_line_id`,
  `delivery_due_lines.import_id`, `app_sessions.user_id` และอื่นๆ)

**เครื่องมือ**

- `npm run typecheck` ใช้ได้แล้วและผ่าน 0 error เดิม `tsc` พัง 10 error
  และไม่มี script ให้รัน จึงไม่มีใครรู้
- `npm test` ผ่านครบ 16/16 เดิมเทสต์ตัวแรกพังด้วย `worker is not a function`
- `npm run check` รวม typecheck + lint + test ไว้ในคำสั่งเดียว
- ลบ script ที่ชี้ไฟล์ที่ไม่มีอยู่ (`db:upgrade:v23`, `db:upgrade:v27`,
  และ `db:generate` ที่เรียก `scripts/sites-env.sh`)

## ยังไม่ได้แก้ในรุ่นนี้

- race condition ตอนจัดงาน (`stage`) ที่ทำให้จัดเกินของจริงได้
- `DB.batch` ตอนตัดยอดไม่ได้ตรวจ `meta.changes` จึงเงียบเมื่อ guard ไม่ผ่าน
- `xlsx@0.18.5` ยังมีช่องโหว่ ต้องย้ายไป SheetJS CDN
- โค้ดตายใน `app/employees/` และตารางรุ่นเก่าใน `db/schema.ts`

---

## v2.8.14 — สรุปยอด Tag สะสมตาม Job

อัปเดต v2.8.14:

- เพิ่มตาราง **สรุปยอด Tag ตาม Job** ในหน้า `พิมพ์ Tag`
- รวม Tag ที่สร้างหลายครั้งของ Job เดียวกันเป็นยอดสะสม แม้ทยอยส่งงานหลายรอบ
- แสดงจำนวน Tag, จำนวนชิ้นรวมจาก Tag, ยอดรอรับเข้า Stock, ยอดรับเข้าแล้ว และเวลาที่สร้างล่าสุด
- กด `ดู Tag ของ Job นี้` เพื่อกรองรายการ Tag ทั้งหมดของ Job นั้นและพิมพ์ซ้ำรายใบได้
- ยอดสะสมไม่นับจำนวนครั้งที่กดพิมพ์ซ้ำ จึงไม่ทำให้ยอด Stock หรือ Due เพิ่มซ้ำ
- ไม่ต้องรัน SQL เพิ่ม

ขั้นตอนอัปเกรดจาก v2.8.13:

1. วางไฟล์ v2.8.14 ตามรายการใน `FILES-v2.8.14.txt` ทับตำแหน่งเดิม
2. Commit และรอ Cloudflare Build/Deploy สำเร็จ
3. เปิดหน้า `พิมพ์ Tag` และค้นหาด้วยเลข Job เพื่อดูยอดสะสม

## v2.8.13 — ค้นหาและพิมพ์ Tag เดิมซ้ำ

อัปเดต v2.8.13:

- เพิ่มช่อง **ค้นหา Tag ที่เคยสร้าง** ในหน้า `พิมพ์ Tag`
- ค้นหาได้จาก Tag ID, Part/Material No., ชื่อชิ้นงาน, ลูกค้า, Job และวันที่ผลิต
- แสดงจำนวนผลลัพธ์และข้อมูลลูกค้าในรายการ เพื่อช่วยตรวจสอบก่อนพิมพ์
- กด **พิมพ์ซ้ำ** เพื่อพิมพ์ Tag เดิมที่สูญหาย โดยคง Tag ID และข้อมูลกล่องเดิม
- การพิมพ์ซ้ำไม่สร้าง Tag ใหม่ ไม่เพิ่มยอด Stock และไม่เปลี่ยนสถานะ Due
- ไม่ต้องรัน SQL เพิ่ม

ขั้นตอนอัปเกรดจาก v2.8.12:

1. วางไฟล์ v2.8.13 ตามรายการใน `FILES-v2.8.13.txt` ทับตำแหน่งเดิม
2. Commit และรอ Cloudflare Build/Deploy สำเร็จ
3. เปิดหน้า `พิมพ์ Tag` แล้วค้นหา Tag เดิมจากช่อง `Tag ที่สร้างแล้ว`

## v2.8.12 — ระบุกล่องเต็มและกล่องเศษบน Tag

อัปเดต v2.8.12:

- Tag พิมพ์ระบุ **FULL BOX / กล่องเต็ม** เมื่อจำนวนในกล่องเท่ากับจำนวนสูงสุดต่อกล่องของ Part
- Tag พิมพ์ระบุ **REMAINDER BOX / กล่องเศษ** เมื่อจำนวนในกล่องน้อยกว่าจำนวนสูงสุดต่อกล่องของ Part
- ใช้ป้ายสีเขียวสำหรับกล่องเต็ม และป้ายสีส้มสำหรับกล่องเศษ เพื่อให้แยกได้ทันที
- ยังคงพิมพ์ได้ 8 Tag ต่อ A4 และคงแบบตัวอักษรใหญ่/สีเข้มจาก v2.8.11
- ไม่ต้องรัน SQL เพิ่ม และไม่กระทบข้อมูล Stock, Due หรือประวัติเดิม

ความสามารถเดิมจาก v2.8.11:

- ยังคงพิมพ์ Stock Tag ได้ **8 Tag ต่อ A4** แบบ 2 คอลัมน์ × 4 แถว
- เพิ่มขนาดตัวอักษรข้อมูลสำคัญ ได้แก่ Customer, Part No., Part Name, จำนวน, กล่อง, Job, วันที่ผลิต และ Tag ID
- เปลี่ยนสีน้ำเงินและสีข้อความให้เข้มขึ้น พร้อมเพิ่มความหนาของเส้นตารางและกรอบ Tag
- บังคับพิมพ์สีด้วย `print-color-adjust: exact` เพื่อให้ Print Preview และงานพิมพ์ใกล้เคียงหน้าจอมากขึ้น
- ไม่ต้องรัน SQL เพิ่ม และไม่กระทบข้อมูล Stock, Due หรือประวัติเดิม

ความสามารถเดิมจาก v2.8.10:

- แบบพิมพ์ A4 รองรับสูงสุด **8 Tag ต่อหน้า**
- จัดวางเป็น **2 คอลัมน์ × 4 แถว** และขึ้นหน้าใหม่อัตโนมัติทุก 8 Tag
- คงรูปชิ้นงาน, QR, Customer, Part No., Part Name, Delivery Qty, จำนวนในกล่อง, วันที่ผลิต, Job และเลขกล่องเหมือนเดิม
- ไม่เปลี่ยนการแบ่งจำนวนต่อกล่อง และ Tag ID ของแต่ละกล่องยังไม่ซ้ำกัน
- ไม่ต้องรัน SQL เพิ่ม

ขั้นตอนอัปเกรดจาก v2.8.9:

1. วางไฟล์ v2.8.12 ตามรายการใน `FILES-v2.8.12.txt` ทับตำแหน่งเดิม
2. Commit และรอ Cloudflare Build/Deploy สำเร็จ
3. เปิดหน้าพิมพ์ Tag แล้วทดลอง Print Preview; 8 Tag จะแสดงในหน้าเดียว และ Tag ที่ 9 จะขึ้นหน้าใหม่

## v2.8.9 — แยกหน้าจัดงาน/ขายออก และปุ่มออกจากระบบบนมือถือ

อัปเดต v2.8.9:

- แยกหน้าเดิม **สแกนและตัดยอด** เป็น 2 เมนูชัดเจน: **จัดงาน** และ **ตรวจและขายออก**
- ผู้จัดงานใช้หน้า **จัดงาน** เพื่อเลือก Due และยิง KIT Stock Tag โดยยังไม่ลด Stock/Due
- ผู้ตรวจใช้หน้า **ตรวจและขายออก** เพื่อยิง Tag ลูกค้า แล้วลด Stock และ Due พร้อมบันทึก Traceability
- Admin กำหนดสิทธิ์สองหน้านี้แยกกันได้เป็นรายบุคคล
- เพิ่มปุ่ม **ออกระบบ** บนแถบเมนูด้านล่างของโทรศัพท์ และในเมนูด้านข้าง
- ต้องรัน `database-upgrade-v2.8.9-split-workflow-permissions.sql` ใน D1 หนึ่งครั้ง เพื่อแปลงสิทธิ์ `scan` เดิม
- SQL ไม่ลบผู้ใช้, Due, Stock, Tag หรือประวัติเดิม

ขั้นตอนอัปเกรดจาก v2.8.8:

1. วางไฟล์ v2.8.9 ตามรายการใน `FILES-v2.8.9.txt` ทับตำแหน่งเดิม
2. Commit และรอ Cloudflare Build/Deploy สำเร็จ
3. เปิด Cloudflare D1 > `kit-stock-db` > Console
4. คัดลอกคำสั่งทั้งหมดจาก `database-upgrade-v2.8.9-split-workflow-permissions.sql` แล้วกด Execute หนึ่งครั้ง
5. เข้าระบบด้วย Admin > ผู้ใช้งาน และตรวจสิทธิ์ **จัดงาน** / **ตรวจและขายออก**

คำสั่งสำหรับผู้ใช้ Wrangler:

```bash
npm run db:upgrade:v289
```

## v2.8.8 — สิทธิ์เข้าใช้งานรายบุคคล

อัปเดต v2.8.8:

- Admin เปิดหน้า **ผู้ใช้งาน** แล้วเลือกสิทธิ์ของแต่ละบัญชีได้ทีละหน้า
- รองรับหน้าหลัก, Stock, พิมพ์ Tag, แผน Due, สแกน, รายการส่งออก, รายงาน, ประวัติ, ตั้งค่า และผู้ใช้งาน
- เมื่อเปลี่ยนบทบาท ระบบเติมชุดสิทธิ์เริ่มต้นให้ แล้ว Admin เปิดหรือปิดเพิ่มเติมได้
- หน้าที่ไม่ได้รับสิทธิ์จะไม่แสดงทั้งเมนูด้านข้างและเมนูมือถือ
- ระบบตรวจสิทธิ์ฝั่ง API สำหรับผู้ใช้งาน, Due, นำเข้า Due, Stock และรูปชิ้นงานด้วย
- ต้องรัน `database-upgrade-v2.8.8-user-permissions.sql` ใน D1 หนึ่งครั้งก่อนบันทึกสิทธิ์
- SQL ใช้ `IF NOT EXISTS` และ `INSERT OR IGNORE` จึงไม่ลบข้อมูลเดิม และเติมสิทธิ์เดิมให้ผู้ใช้ปัจจุบัน

ขั้นตอนอัปเกรดจาก v2.8.7:

1. วางไฟล์ v2.8.8 ทับในโฟลเดอร์ระบบเดิม
2. Commit ให้ Cloudflare Build/Deploy สำเร็จ
3. เปิด Cloudflare D1 > `kit-stock-db` > Console
4. คัดลอกคำสั่งทั้งหมดจาก `database-upgrade-v2.8.8-user-permissions.sql` แล้วกด Execute หนึ่งครั้ง
5. เข้าระบบด้วย Admin > ผู้ใช้งาน > แก้ไข / สิทธิ์ / PIN

คำสั่งสำหรับผู้ใช้ Wrangler:

```bash
npm run db:upgrade:v288
```

อัปเดต v2.8.7:

- เพิ่มปุ่ม `เปิดกล้องยิง Tag` บนหน้ารับงานเข้า Stock
- ใช้กล้องหลังของโทรศัพท์อ่าน QR ของ KIT Stock Tag
- เมื่ออ่าน QR สำเร็จ ระบบรับ Tag เข้า Stock ทันทีโดยไม่ต้องกดปุ่มซ้ำ
- ปุ่มกล้องในหน้าจัดงาน/ขายออกยังทำงานตามโหมดเดิม
- ไม่ต้องรัน SQL เพิ่ม

อัปเดต v2.8.6:

- แยกเมนู `Stock` และ `พิมพ์ Tag` ออกจากกันอย่างชัดเจน
- หน้า `พิมพ์ Tag` รวมทะเบียน Part, นำเข้า Part Excel, สร้าง Tag, พิมพ์ซ้ำ และลบ Tag ที่ยังไม่รับเข้า
- หน้า `Stock` รวมสแกนรับเข้า, ยอดคงเหลือ, รายการ Stock และ Traceability
- ใช้ฐานข้อมูล Part และ Tag ชุดเดิม จึงไม่ต้องรัน SQL เพิ่ม

อัปเดต v2.8.5:

- Admin นำเข้าทะเบียน Part จาก Excel ได้ โดยรองรับคอลัมน์ `Part / Material No.`, `Part Name`, `Customer` และ `Max Qty per Box`
- Tag รับเข้า Stock แสดงรูปชิ้นงานและ QR/Barcode ในพื้นที่ด้านบน
- Tag แสดง Customer, Part No./Material, Part Name, Delivery Qty, วันที่ผลิต, Job, จำนวนในกล่อง และเลขกล่อง
- ยังคงแบ่ง Tag ตามจำนวนสูงสุดต่อกล่องอัตโนมัติ และใน v2.8.10 พิมพ์ได้สูงสุด 8 Tag ต่อ A4
- ไม่ต้องรัน SQL เพิ่มจาก v2.8.4

อัปเดต v2.8.4: ในทะเบียน Part ต้องกำหนด **จำนวนสูงสุดต่อกล่อง** และเมื่อสร้าง Tag ให้กรอกจำนวนงานรวมของ Job ระบบจะแบ่งเป็นกล่องและสร้าง Tag จริงให้ทุกกล่องอัตโนมัติ

- กล่องเต็มใช้จำนวนสูงสุดที่กำหนดใน Part และกล่องสุดท้ายเป็นจำนวนเศษ
- แต่ละกล่องมี QR และ Tag ID ไม่ซ้ำกัน พร้อมเลข `กล่อง 1/6`, `2/6` จนครบ
- หน้าพิมพ์ A4 ของ v2.8.10 วางได้สูงสุด 8 Tag ต่อหน้า (2 คอลัมน์ × 4 แถว) และขึ้นหน้าใหม่อัตโนมัติเมื่อเกิน 8 กล่อง
- เอาช่อง **Status** ออกจากแบบพิมพ์ Tag แล้ว
- Part ที่นำมาจาก Due แต่ยังมีค่า 0 ต้องให้ Admin กำหนดจำนวนสูงสุดต่อกล่องก่อนสร้าง Tag
- ปุ่มลบ Tag เดิมยังอยู่ และยังลบได้เฉพาะ Tag ที่ยังไม่เคยรับเข้า Stock
- ไม่ต้องรัน SQL เพิ่มสำหรับ v2.8.4

## ตัวอย่างการแบ่งกล่อง

ถ้า Job มี 1,000 ชิ้น และ Part บรรจุได้สูงสุด 180 ชิ้นต่อกล่อง ระบบจะสร้าง 6 Tag:

`180 + 180 + 180 + 180 + 180 + 100`

Tag จะแสดงกล่อง `1/6` ถึง `6/6` และจำนวนจริงของแต่ละกล่อง

อัปเดต v2.8.3: หน้าพิมพ์ A4 หนึ่งหน้าจัดวาง Tag รับงานเข้า Stock จำนวน 6 ดวง (2 คอลัมน์ × 3 แถว) โดยคงรูปชิ้นงานและข้อมูลเดิมทั้งหมด และเพิ่มปุ่มให้ Admin ลบ Tag ที่พิมพ์ผิดหรือไม่ได้ใช้งานได้

- Tag ทั้ง 6 ดวงในหน้าพิมพ์เป็นสำเนาของ Tag Stock เดียวกันและใช้ QR/Tag ID เดียวกัน
- ปุ่ม **ลบ Tag** แสดงเฉพาะ Admin และเฉพาะ Tag สถานะ **รอรับเข้า**
- Tag ที่รับเข้า Stock, จัดงาน หรือขายออกแล้วจะลบไม่ได้ เพื่อรักษายอด Stock, Due และ Traceability
- ไม่ต้องรัน SQL เพิ่มสำหรับ v2.8.3

ขั้นตอน Stock, ข้อมูล QR, การรับเข้า, การจัดงาน, การขายออก, การตัด Stock/Due และหน้าจออื่นคงเดิมจาก v2.8.1

อัปเดต v2.8.1: Admin สามารถค้นหาและลบ Part ที่ยังไม่เคยสร้าง Tag หรือมีประวัติ Stock ได้ ทั้งแบบทีละรายการและลบรายการที่ยังไม่ใช้งานทั้งหมด

ขั้นตอนอัปเกรดและวิธีใช้งาน v2.8 อยู่ในไฟล์ `README-v2.8-TRACEABILITY.txt`

ชุดนี้ทำสำหรับ Worker เดิมชื่อ `kit-stock-system` โดยเฉพาะ

URL เดิม:

```text
https://kit-stock-system.kishimoto-th.workers.dev
```

## สิ่งที่ชุดอัปเกรดจะทำ

- เปลี่ยนหน้าเว็บเก่าเป็นระบบ Delivery Due Control ใหม่
- ใช้ Binding เดิมชื่อ `DB` และ `BUCKET`
- เก็บ D1 Database และ R2 Bucket เดิมไว้
- ไม่ลบตารางหรือข้อมูลของระบบ Stock เก่า
- เพิ่มตาราง Due, QR Tag, ผู้ใช้งาน และ Session ที่ระบบใหม่ต้องใช้
- เพิ่ม Due 706 รายการ รวม 69,385 ชิ้น โดยป้องกันข้อมูลซ้ำ
- Admin เพิ่มผู้ใช้งานได้หลายคนจากหน้า **ผู้ใช้งาน**
- กำหนดบทบาท **ผู้จัดงาน** (สแกนรับเข้า) หรือ **ผู้ตรวจงาน** (สแกนส่งออกและตัด Due)
- ตั้ง/รีเซ็ต PIN 6 หลัก เปิดใช้หรือระงับบัญชีได้จากหน้าเว็บ
- PIN ของผู้ใช้ใหม่จัดเก็บแบบ PBKDF2 Hash ไม่เก็บเป็นข้อความปกติ
- สแกนแล้วบันทึกและแสดงข้อมูลทันที ไม่ต้องกดปุ่มตรวจสอบ Tag
- แสดงรูปชิ้นงานทันทีพร้อมข้อมูล Tag/Due หลังสแกน ทั้งรับเข้าและส่งออก
- Admin ผูกรูป JPG, PNG หรือ WebP กับ Material / Part No. ได้จากหน้า **ตั้งค่า**
- เก็บไฟล์รูปใน R2 เดิม และเก็บรายการเชื่อม Material ใน D1 เดิม
- หน้า **รายงาน** ส่งออกข้อมูลตามตัวกรองปัจจุบันได้เป็น Excel, CSV และ PDF
- Excel มีชีตสรุปและชีตรายละเอียด Due ส่วน PDF จัดหน้าแนวนอนสำหรับพิมพ์หรือบันทึกเป็นไฟล์
- Admin ลบข้อมูล Excel เป็นรายชุดได้จากหน้า **แผนส่งงาน (Due) > ประวัตินำเข้า Excel**
- ก่อนลบมีหน้าต่างยืนยัน และหากชุดนั้นมีประวัติรับเข้า/ส่งออก ระบบจะเตือนซ้ำพร้อมจำนวนรายการ
- การลบกระทบเฉพาะชุดไฟล์ที่เลือก ไม่ลบผู้ใช้งาน รูปชิ้นงาน หรือ Due จากไฟล์ชุดอื่น
- หน้าโทรศัพท์จัดวางใหม่ให้อ่านง่ายโดยไม่ต้องซูมหรือเลื่อนตารางซ้าย–ขวา
- ตาราง Due, รายการสแกน, รายงาน และผู้ใช้งานเปลี่ยนเป็นการ์ดบนหน้าจอเล็ก
- มีเมนูหลักด้านล่างสำหรับใช้งานด้วยนิ้วโป้ง และเปิดเมนูทั้งหมดจากปุ่ม **เมนู**
- หน้าสแกนย่อกรอบกล้องให้พอดีจอ ปุ่มสแกนมีขนาดใหญ่ และหลังสแกนจะเลื่อนไปยังรูปชิ้นงานกับยอด Due อัตโนมัติ
- ตัวกรองบนมือถือพับเก็บได้เพื่อเพิ่มพื้นที่แสดงข้อมูล

## สำคัญก่อนวางไฟล์ทับ

เก็บไฟล์ Cloudflare เดิมต่อไปนี้ไว้ ห้ามลบ:

- `wrangler.jsonc` หรือ `wrangler.json` หรือ `wrangler.toml`
- `.git` หากทำงานผ่าน GitHub บนเครื่อง

ไฟล์ `wrangler.overlay.example.jsonc` เป็นเพียงตัวอย่าง ไม่ต้องนำไปแทนไฟล์ Wrangler เดิม เพราะไฟล์เดิมมีรหัสเชื่อม D1/R2 ของบริษัทอยู่แล้ว

## ถ้าอัปโหลดผ่าน GitHub ตามภาพ

Repository ของคุณคือ `kishimotoindustrythailandk-bit/kit-stock-system`

1. แตก ZIP นี้บนคอมพิวเตอร์
2. เข้าโฟลเดอร์ชั้นในสุดของระบบเก่าที่มี `app`, `db`, `public`, `worker` และ `package.json`
3. วางไฟล์จาก ZIP ทับโฟลเดอร์นั้น และเลือก **Replace files**
4. อย่าสร้างโฟลเดอร์ `kit-stock-system-v1` ซ้อนเพิ่มอีกชั้น
5. Upload/Commit ไฟล์ที่แก้ไขกลับไปยังตำแหน่งเดิมใน GitHub
6. Cloudflare จะ Build และ Deploy ทับ Worker `kit-stock-system` โดย URL เดิมไม่เปลี่ยน

หากระบบปัจจุบันเป็น **v2.3, v2.4 หรือ v2.5 อยู่แล้ว ไม่ต้องรัน SQL เพิ่ม** สำหรับ v2.6 นี้ ให้วางไฟล์ทับและ Commit ได้เลย

เฉพาะกรณีอัปเกรดจากเวอร์ชันก่อน v2.3 เท่านั้น หลังโค้ด Deploy แล้วให้รัน `database-upgrade-v2.2-users-scan.sql` และ `database-upgrade-v2.3-part-images.sql` อย่างละหนึ่งครั้ง (ไฟล์ใช้ `IF NOT EXISTS` และไม่ลบข้อมูลเดิม)

## วิธีแนะนำ: อัปเกรดจากคอมพิวเตอร์ Windows

วิธีนี้จะอัปเกรด D1 และ Deploy ให้ครบอัตโนมัติ

1. สำรองโฟลเดอร์ระบบเก่าไว้หนึ่งชุด
2. วางไฟล์จาก ZIP ทับโฟลเดอร์ระบบเก่า
3. ดับเบิลคลิก `INSTALL-WINDOWS.bat`
4. Login Cloudflare เมื่อเบราว์เซอร์เปิด
5. ตั้ง PIN ใหม่สำหรับรหัสพนักงาน `ADMIN`

ตัวติดตั้งจะใช้ Binding เดิม ไม่สร้าง D1 หรือ R2 ใหม่

## อัปเกรดด้วยคำสั่งเอง

```bash
npm install
npx wrangler login
npm run db:upgrade
npm run db:upgrade:v22
npm run db:upgrade:v23
npm run deploy
npx wrangler secret put INITIAL_ADMIN_PIN --config dist/server/wrangler.json
```

## เข้าสู่ระบบใหม่

- รหัสพนักงาน: `ADMIN`
- PIN: ค่าที่ตั้งตอนใช้คำสั่ง `INITIAL_ADMIN_PIN`

จากนั้นเข้าเมนู **ผู้ใช้งาน** แล้วกด **เพิ่มผู้ใช้งาน**:

1. ระบุรหัสพนักงาน ชื่อ และ PIN 6 หลัก
2. เลือก `ผู้จัดงาน — สแกนรับเข้า` หรือ `ผู้ตรวจงาน — สแกนส่งออก/ตัด Due`
3. ผู้ใช้เข้าสู่ระบบด้วยรหัสพนักงานและ PIN ของตัวเองได้ทันที
4. หากต้องหยุดสิทธิ์ ให้ Admin กด **ระงับ** ระบบจะยกเลิก Session ของคนนั้นทันที

## เพิ่มรูปชิ้นงานให้แสดงตอนสแกน

1. เข้าระบบด้วย Admin และเปิดเมนู **ตั้งค่า**
2. ที่หัวข้อ **รูปชิ้นงานสำหรับหน้าสแกน** ระบุ Material / Part No.
3. เลือกรูป JPG, PNG หรือ WebP ขนาดไม่เกิน 5 MB แล้วกด **บันทึกรูปชิ้นงาน**
4. เมื่อผู้จัดงานหรือผู้ตรวจงานสแกน Tag ที่มี Material เดียวกัน รูปจะแสดงทันทีพร้อมข้อมูล Due
5. หากยังไม่มีรูป ระบบจะแสดงช่องสำรอง “ยังไม่มีรูป” โดยการสแกนและตัดยอดยังทำงานตามปกติ

## สำรองข้อมูลก่อนอัปเกรด

แนะนำให้เปิด Cloudflare > Storage & databases > D1 > ฐานข้อมูลเดิม > Backups และสร้าง Backup ก่อน แม้ไฟล์อัปเกรดนี้ไม่มีคำสั่งลบข้อมูล

## ไฟล์ข้อมูลต้นฉบับ

โฟลเดอร์ `source-files` มี Excel ต้นทางสองไฟล์และ PDF Tag ตัวอย่าง ไฟล์ PDF ไม่ถูกนำไปตัดยอดอัตโนมัติ
